# 皮肤新增指南 — 如何给 dsh 管理器加新皮肤

本文说明在已装好的 dsh 皮肤管理机制上，**以后再增加新皮肤**要做什么。
核心结论：装一次（复制 + 注册 + 重启），以后在 设置 > 外观 · 皮肤 里随便切，永不重启。

---

## 最快的路：一条命令

```powershell
# 从本地目录安装
.\install-skin.ps1 -PackageDir "C:\path\to\皮肤包目录"

# 从 GitHub 仓库安装（自动 clone；皮肤在子目录时用 -SubDir）
.\install-skin.ps1 -Repo "https://github.com/SomeOne/dsh-a-new-skin" [-SubDir "maid-atelier"]

# 装完立刻作为当前皮肤
.\install-skin.ps1 -PackageDir "..." -Active

# 只想看它会做什么（不写入）
.\install-skin.ps1 -PackageDir "..." -WhatIf
```

脚本做的事：
1. 复制皮肤包 → `~/.dsh/profiles/web/node_modules/@dsh-external/<包名>/`
2. 在 profile 补丁的受管注册区追加一行加载行（幂等，重复运行不会重复加）
3. 在 home 补丁追加该皮肤的 `disabled` 标志（默认停用，`-Active` 则启用）
4. 提示你**重启一次服务**（注册行是安装期操作）

重启后打开 设置 > 外观 · 皮肤，新皮肤就会出现在卡片列表里，可直接切换。

---

## 皮肤包的最小契约

想让管理器识别并切换，皮肤包需要满足：

```text
皮肤包/
├── package.json         # 包定义（必须）
├── skin.json            # 管理器卡片元数据（必须，否则列表里不显示）
├── lib/
│   ├── index.js         # 宿主半（可选，空 apply 即可）
│   └── client.js        # 浏览器皮肤包（必须，exports["./client"] 指向它）
└── preview/
    ├── light.webp       # 浅色预览图（管理器卡片用）
    └── dark.webp        # 深色预览图（可选）
```

**package.json 关键字段：**

```jsonc
{
  "name": "@dsh-external/dsh-client-ui-skin-<short>",   // 行 id 由此推导：ui-skin-<short>
  "type": "module",
  "exports": {
    "./client": "./lib/client.js"
  },
  "dsh": {
    "client": {
      "platform": "web",
      "inject": ["@deepseek-ai/dsh-client-ui-theme"]     // 皮肤需要的客户端服务/模块
    }
  }
}
```

**skin.json 关键字段：**

```jsonc
{
  "id": "unique-skin-id",            // 皮肤逻辑 id（与行 id 无关，勿与其它皮肤重复）
  "name": "皮肤中文名",
  "nameEn": "Skin English Name",
  "tagline": "一句话介绍",
  "accent": "#c5a468",               // 卡片上的强调色
  "preview": { "light": "preview/light.webp", "dark": "preview/dark.webp" },
  "order": 10                        // 列表排序
}
```

> ⚠️ 两个常见坑：
> 1. **行 id 冲突**：行 id = `ui-skin-<包名短名>`，是全局唯一键。别照抄别的皮肤的 `wiring.id`（例如上游昼夜皮肤自带的行 id 就是 `ui-skin-maid-atelier`，会和女仆工坊撞 → duplicate loader entry id）。用本脚本安装就会自动按包名推导出唯一 id。
> 2. **UTF-8 无 BOM**：所有 JSON/YAML 文件不要带 BOM，否则解析失败。用 PowerShell 7 的 `Set-Content -Encoding utf8` 写入即无 BOM。

---

## 手动安装（不跑脚本的完整步骤）

1. **复制**：把皮肤包整个目录拷到
   `~/.dsh/profiles/web/node_modules/@dsh-external/<包名>/`
   （包名要与 package.json name 一致，如 `dsh-client-ui-skin-my-new-theme`）
2. **注册行**：编辑 `~/.dsh/profiles/web/cordis.patch.yml`，在受管注册区（`# --- dsh-skin registry rows ... ---` 之间）的 insert 列表里追加：

   ```yaml
       - id: ui-skin-my-new-theme
         name: '@dsh-external/dsh-client-ui-skin-my-new-theme'
   ```

3. **停用标志**（可选，默认让它不自动生效）：编辑 `~/.dsh/cordis.patch.yml` 受管区，追加：

   ```yaml
   - id: ui-skin-my-new-theme
     disabled: true
   ```

4. **重启一次服务**，刷新页面 → 管理器列表里出现新皮肤，之后切换与其它皮肤一致（无需再重启）。

> 手动编辑时保持两个受管区之间（`# --- end ... ---`）之外的任何用户内容原样不动。

---

## 为什么有的操作要重启一次

- **切换皮肤 / 恢复官方**：只改 home 层的 `disabled` 标志 → HMR 热应用 → 页面自动刷新。**永不重启。**
- **新增皮肤的"注册行"**：在 profile 层插入新行，属于安装期操作，一次重启后永久生效（此后该皮肤与其它皮肤一样热切换）。
- **改动皮肤宿主端代码（lib/index.js）**：Node 对 loader 条目只 import 一次并缓存，rc.7 没有宿主模块热重载，需要重启。
- **改动皮肤客户端代码（lib/client.js）**：dev 模式会重建 bundle；刷新页面即可（无需重启服务）。

---

## 给皮肤作者的转换速查

把一个自定义主题转成皮肤包，最小骨架：

```powershell
# 在某个空目录里
mkdir .\lib .\preview
# 写 package.json（见上）、skin.json（见上）
# lib/client.js 直接写你的样式应用逻辑，例如：
```

```js
// lib/client.js —— 挂载时生效；管理器靠"启用=挂载"来控制显示
window.__ModuleLoader__.load({
  id: "@dsh-external/dsh-client-ui-skin-my-new-theme",
  factory: (require) => {
    const module = { exports: {} };
    function apply(ctx) {
      // 这里做你的皮肤应用：注入 <style>、设置 data-* 属性等
    }
    module.exports.apply = apply;
    return module.exports;
  }
});
```

宿主半 `lib/index.js`（最小）：

```js
function apply() {}
export { apply };
```

把预览图放进 preview/，然后 `.\install-skin.ps1 -PackageDir "该目录"` 即可。

---

## 版权提醒

皮肤资源版权归原作者。从 GitHub 安装他人作品前确认许可证（常见 CC BY-NC-SA 4.0 仅限个人非商业用途），保留 NOTICE/LICENSE 与署名。管理器本身（dsh-client-ui-skin-manager）是 MIT。

## 不支持的场景

- 需要运行 `dsh plugin add` 命令来装皮肤的发行形态：本机制是复制安装，不经过 `dsh plugin` 命令注册（避免与 rc.8+ 的官方插件命令重叠造成行冲突；未来官方演进后可平滑迁移）。
- 依赖 `themeCatalog`（rc.6 不存在、rc.7 由 rc7-bridge 兜底）的皮肤：请使用自包含或 themePlugins 适配形态（现有两款皮肤就是这两种形态的参照）。